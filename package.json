{
  "name": "rmmd",
  "version": "0.0.1",
  "engines": {
    "node": "6.11.1",
    "npm": "3.10.10"
  },
  "scripts": {
    "heroku-postbuild": "cd Painteger.Frontend && npm install && npm run build:production"
  }
}
{
  "name": "rmmd",
  "version": "0.0.1",
  "engines": {
    "node": "14.11.0",
    "npm": "3.10.10"
  },
  "scripts": {
    "heroku-postbuild": "cd Painteger.Frontend && npm install && npm run build"
  }
}
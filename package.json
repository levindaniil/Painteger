{
  "name": "rmmd",
  "version": "0.0.1",
  "engines": {
    "node": "14.x",
    "npm": "6.x"
  },
  "scripts": {
    "heroku-prebuild": "npm clean cache —force",
    "heroku-postbuild": "cd Painteger.Frontend && npm install && npm run build"
  }
}
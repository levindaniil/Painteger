import React from 'react';
import Hero from "./Hero";

function Main(props) { // TODO: переделать в класс
    return (
        <main>
            <Hero />
        </main>
    );
}

export default Main;
